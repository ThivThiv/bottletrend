<div class="container">
  <h1>Ma collection</h1>
  <section class="vgv">
    <h2>Dashboard</h2>
  </section>

<%# Faire partial %>
  <section>
    <h3>Mes possessions</h3>
    <% @private_bottles.each do |batch, qty| %>
      <div class="card">
        <h4><%= batch.name %></h4>
        <h5><%= batch.domain.name %></h5>
        <p>Quantité: <%= qty %></p>
        <p><%= batch.initial_price * qty %>€</p>
      </div>
    <% end %>

    <h3>Mes ventes</h3>
    <% @on_resale_bottles.each do |batch, qty| %>
      <div class="card">
        <h4><%= batch.name %></h4>
        <h5><%= batch.domain.name %></h5>
        <p>Quantité: <%= qty %></p>
        <p><%= batch.initial_price * qty %>€</p>
      </div>
    <% end %>
  </section>
</div>
